{% extends 'base.html.twig' %}

{% block title %}Symfony6{% endblock %}

{% block body %}
    {{ form(form) }}

    {{ dump(posts) }}

    {# es un comentario #}

     {# {{ }} <- para variables y para funciones #}

    {# {% %} <- para operaciones logincas , declaracion de variables #}

    {#  {{ 'welcome'|upper }} #}
    <table>
        <thead>
            <tr>
                <td>ID</td>
                <td>titulo</td>
                <td>descripción</td>
                <td>Creation date</td>
            </tr>
        </thead>
        <tbody>
        {%  for post in posts %}
            <tr>
                <td>{{ post.id }}</td>
                <td>{{ post.title }}</td>
                <td>{{ post.description }}</td>
                <td>{{ post.creation_date |date("m/d/Y") }}</td>
            </tr>
        {% endfor %}
        </tbody>

        </table>


{% endblock %}
